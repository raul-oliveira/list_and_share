import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:list_and_share/app/modules/my_lists/my_lists_module.dart';

void main() {
  initModule(MyListsModule());
  // ListEditController listedit;
  //
  setUp(() {
    //     listedit = MyListsModule.to.get<ListEditController>();
  });

  group('ListEditController Test', () {
    //   test("First Test", () {
    //     expect(listedit, isInstanceOf<ListEditController>());
    //   });

    //   test("Set Value", () {
    //     expect(listedit.value, equals(0));
    //     listedit.increment();
    //     expect(listedit.value, equals(1));
    //   });
  });
}
