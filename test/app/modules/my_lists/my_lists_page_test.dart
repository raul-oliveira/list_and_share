import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MyListsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MyListsPage(title: 'MyLists')));
    //  final titleFinder = find.text('MyLists');
    //  expect(titleFinder, findsOneWidget);
  });
}
