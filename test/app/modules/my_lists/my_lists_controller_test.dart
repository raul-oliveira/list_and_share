import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:list_and_share/app/modules/my_lists/my_lists_module.dart';

void main() {
  initModule(MyListsModule());
  // MyListsController mylists;
  //
  setUp(() {
    //     mylists = MyListsModule.to.get<MyListsController>();
  });

  group('MyListsController Test', () {
    //   test("First Test", () {
    //     expect(mylists, isInstanceOf<MyListsController>());
    //   });

    //   test("Set Value", () {
    //     expect(mylists.value, equals(0));
    //     mylists.increment();
    //     expect(mylists.value, equals(1));
    //   });
  });
}
